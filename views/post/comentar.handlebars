{{{success}}}
<div class="container">
    <div class="container">
        <div class="container form-control mb-3">
            <form action="/{{nome}}/{{id}}/comentar" method="POST">
                <div class="mb-3">
                    <label for="comentario" class="form-label"><strong>Seu comentario</strong></label>
                    <textarea class="form-control" name="comentario" id="comentario" style="height: 211px;" rows="3"></textarea>
                </div>
                <div class="mb-3">
                    <button type="submit" class="btn btn-success">
                        <strong>Comentar</strong>
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>