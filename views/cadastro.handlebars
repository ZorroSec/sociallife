<div class="container" style="margin-top: 3.3em;">
    <form action="/cadastro" method="POST">
        <h2>
            Cadastrar novo usuario
        </h2>
        <div class="mb-3">
            <p class="form-text text-muted"><strong>Campos marcados com asterisco (*) são obrigatorios</strong></p>
            
        </div>
        {{{validationMsg}}}
        
        
        <div class="mb-3">
            <label for="nome" class="form-label">Nome <strong>*</strong></label>
            <input type="text" name="nome" id="nome" class="form-control" placeholder="Seu nome" aria-describedby="nameHelp">
            <smal id="nameHelp" class="form-text text-muted">
                Ex: joaocorreia
            </smal>
        </div>
        <div class="mb-3">
            <label for="email" class="form-label">Email <strong>*</strong></label>
            <input
                type="email"
                class="form-control"
                name="email"
                id="email"
                aria-describedby="emailHelpId"
                placeholder="abc@mail.com"
            />
            <small id="emailHelpId" class="form-text text-muted">Não iremos compartilhar seu email com mais, ninguém</small>
        </div>
        <div class="mb-3">
            <label for="senha" class="form-label">Senha <strong>*</strong></label>
            <input type="password" class="form-control" name="senha" id="senha" placeholder="Senha" />
        </div>
        <div class="mb-3">
            <label for="descricao" class="form-label">Descrição</label>
            <textarea class="form-control" name="descricao" id="descricao" rows="3">Olá, amigoos...</textarea>
        </div>
        <div class="mb-3 d-flex justify-content-center">
            <p class="form-text">Já tem uma conta? clique <a href="/login">Aqui</a> ara acessar a sua conta.</p>
        </div>
        <button type="submit" class="btn btn-success" onclick="{{ onClick }}">
            Cadastrar
        </button>
    </form>
</div>
